import React from "react";

const Footer = ({ length }) => {
  return (
    <footer className="footer">
     Work to done is : {length}
      
    </footer>
  );
};

export default Footer;
